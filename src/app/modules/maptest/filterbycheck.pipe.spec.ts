import { FilterbycheckPipe } from './filterbycheck.pipe';

describe('FilterbycheckPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbycheckPipe();
    expect(pipe).toBeTruthy();
  });
});
