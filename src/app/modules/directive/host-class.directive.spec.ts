import { HostClassDirective } from './host-class.directive';

describe('HostClassDirective', () => {
  it('should create an instance', () => {
    const directive = new HostClassDirective();
    expect(directive).toBeTruthy();
  });
});
